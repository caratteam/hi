name: Deploy Mom Bot

on:
  push:
    branches: [hansol]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Lightsail
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ubuntu
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          script: |
            source ~/.mom-env
            cd ~/hi
            git fetch origin hansol
            git checkout hansol
            git pull origin hansol
            npm install
            cd packages/mom
            ./start.sh
